/**
 * Tool keys for the JumpDoc PDF editor toolbar.
 *
 * Dynamic keys (built from doc state):
 *   "origin-{catId}"     — draw an origin in that category
 *   "purchase-{subtypeId}" — draw a purchase for that subtype
 * Static keys:
 *   "drawback"
 *   "scenario"
 */
export type ToolType = string;
